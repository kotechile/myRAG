version: '3.8'

services:
  rag-system:
    build:
      context: .
      dockerfile: Dockerfile
    # container_name removed to allow Coolify to manage naming/scaling
    expose:
      - "8080"
    environment:
      - PORT=8080
      - FLASK_ENV=production
      # All other env vars should be injected by Coolify from .env

    volumes:
      - ./uploads:/app/uploads
    restart: always

    # Explicitly configure Traefik for Port 8080 to ensure routing
    # AND explicitly enable the coolify network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=coolify"
      - "traefik.http.routers.rag-system.rule=Host(`rag.aichieve.net`)"
      - "traefik.http.routers.rag-system.entrypoints=websecure"
      - "traefik.http.routers.rag-system.tls.certresolver=letsencrypt"
      - "traefik.http.services.rag-system.loadbalancer.server.port=8080"

    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://127.0.0.1:8080/health || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 20s

    networks:
      - coolify
      - default

networks:
  coolify:
    external: true
